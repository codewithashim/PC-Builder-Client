import React from 'react';
import HomeSlider from '../HomeSlider/HomeSlider';

const HeroSection = () => {
    return (
        <section>
            <HomeSlider />
        </section>
    );
};

export default HeroSection;